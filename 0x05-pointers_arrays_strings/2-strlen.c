#include "main.h"

/**
 * _strlen - is a function that returns the length of a string
 * @s: parameter
 */

int _strlen(char *s)
{
	int length = 0;

	while (*s != '\0')
	{
		lenth++;
		s++;
	}

	return (lenght);
}
