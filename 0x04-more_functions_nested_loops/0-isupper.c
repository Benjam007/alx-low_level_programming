#include "main.h"

/**
 * _isupper - checks for uppercase letters
 * @c: parameter
 * Return: returns 0 if succesful
 */

int _isupper(int c)
{
if (c >= 'A' && <= 'Z')
{
return (1);
}
else
{
return (0);
}
return (0);
}
